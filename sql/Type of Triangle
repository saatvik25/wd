select case
when A = B and B = C and A = C then 'Equilateral'
when A + B = C or C + B = A or  C + A = B then 'Not A Triangle'
when A = B or B = C OR A = C then 'Isosceles' 
when A + B > C AND C + B > A AND A + C > B THEN 'Scalene'
else 'Not A Triangle'
end from Triangles;
