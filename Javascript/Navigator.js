//The javaEnabled() method returns a Boolean value 
//that specifies whether the browser has Java enabled.

let java = navigator.javaEnabled();
console.log(java)

true if the browser has Java enabled, otherwise false.

